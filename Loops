Question:Task
The provided code stub reads and integer,n , from STDIN. For all non-negative integers i <n, print  square(i).


#-------------------Solution----------------------

n = int(input())

for i in range(n):
    if(i < n):
        print(pow(i,2))
